#include "Commands.h"
#include "Model.h"

using namespace Commands;

void ResetNPC::execute(Model& model, Reset& reset) {
		// int limit = reset.getLimit();
		// NPC npc = model.NPCs[reset.getId()];

		// //NPC npcClone = clone(npc); 
		// model.rooms[reset.getRoom()].addNPC(npc, limit);
}